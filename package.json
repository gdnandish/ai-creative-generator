{
  "name": "deploy-ai-creative-generator",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "vercel dev",
    "build": "echo \"No build step required\"",
    "start": "echo \"Use Vercel deployment or vercel dev locally.\""
  },
  "engines": {
    "node": ">=20"
  }
}
